{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit  col-lg-4 col-lg-offset-4' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="test" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}
